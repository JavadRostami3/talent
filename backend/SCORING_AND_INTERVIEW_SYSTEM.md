# 📊 سیستم امتیازدهی و مصاحبه - استعداد درخشان دکتری

## 🎯 **بخش 1: امتیازدهی اطلاعات تحصیلی**

### **جدول امتیازدهی کامل**

#### **الف) معدل و کیفیت دانشگاه تحصیل دوره کارشناسی (تا 6 امتیاز):**

| معدل | امتیاز معدل |
|------|--------|
| 19.00 - 17.50 | 6 امتیاز |
| 17.49 - 16.00 | 5 امتیاز |
| 15.99 - 14.00 | 2 امتیاز |
| 13.99 - 12.00 | 3 امتیاز |
| زیر 12 | 0 امتیاز |

**امتیاز دانشگاه:** بر اساس ضریب دانشگاه در جدول `UniversityWeight`

#### **ب) معدل و کیفیت دانشگاه تحصیل دوره کارشناسی ارشد (تا 5 امتیاز):**

| معدل | امتیاز معدل |
|------|--------|
| 19.00 - 17.50 | 5 امتیاز |
| 17.49 - 16.00 | 4 امتیاز |
| 15.99 - 14.00 | 2 امتیاز |
| 13.99 - 12.00 | 2 امتیاز |
| زیر 12 | 0 امتیاز |

**امتیاز دانشگاه:** بر اساس ضریب دانشگاه

#### **ج) طول مدت دوره کارشناسی (تا 3 امتیاز):**

| طول مدت | امتیاز |
|---------|--------|
| 8 ترم یا کمتر | 3 امتیاز |
| 9 ترم | 2 امتیاز |
| 10 ترم | 1 امتیاز |
| بیشتر از 10 ترم | 0 امتیاز |

#### **د) طول مدت دوره کارشناسی ارشد (تا 3 امتیاز):**

| طول مدت | امتیاز |
|---------|--------|
| 4 ترم | 3 امتیاز |
| 5 ترم | 2 امتیاز |
| 6 ترم | 1 امتیاز |
| بیشتر از 6 ترم | 0 امتیاز |

#### **ه) برگزیدگان المپیادهای علمی (تا 5 امتیاز):**

| رتبه المپیاد | امتیاز |
|--------------|--------|
| رتبه طلا (المپیاد بین‌المللی) | 5 امتیاز |
| رتبه نقره (المپیاد بین‌المللی) | 4 امتیاز |
| رتبه برنز (المپیاد بین‌المللی) | 3 امتیاز |
| رتبه طلا (المپیاد کشوری) | 4 امتیاز |
| رتبه نقره (المپیاد کشوری) | 3 امتیاز |
| رتبه برنز (المپیاد کشوری) | 2 امتیاز |
| 10 نفر برتر | 2 امتیاز |
| 20 نفر برتر | 1 امتیاز |

#### **و) مدرک زبان معتبر (تا 8 امتیاز):**

**زبان انگلیسی:**

| نوع آزمون | نمره | امتیاز |
|----------|------|--------|
| **TOEFL iBT** | 100-120 | 8 امتیاز |
| | 90-99 | 6 امتیاز |
| | 80-89 | 4 امتیاز |
| | 70-79 | 2 امتیاز |
| **IELTS Academic** | 7.5-9 | 8 امتیاز |
| | 6.5-7 | 6 امتیاز |
| | 6-6.5 | 4 امتیاز |
| | 5.5-6 | 2 امتیاز |
| **GRE** | Verbal 160+ | 8 امتیاز |
| | Verbal 150-159 | 6 امتیاز |
| **MSRT** | 60-100 | 6 امتیاز |
| | 50-59 | 4 امتیاز |
| | 40-49 | 2 امتیاز |

**سایر زبان‌ها:** بر اساس سطح و نمره (تا 8 امتیاز)

---

### **مدل ApplicationEducationRecord**

```python
class ApplicationEducationRecord:
    # اطلاعات تحصیلی
    degree_level: BSC / MSC / PHD
    university: ForeignKey
    field_of_study: str
    gpa: Decimal
    start_month, start_year: int
    graduation_month, graduation_year: int
    status: GRADUATED / STUDYING
    
    # ============================================
    # امتیازدهی توسط کارشناس (برای دکتری)
    # ============================================
    gpa_score: float = 0  # امتیاز معدل (طبق جدول)
    university_score: float = 0  # امتیاز دانشگاه
    total_score: float = 0  # جمع امتیازها
    
    # بررسی
    reviewed_by: User
    reviewed_at: DateTime
    review_comment: Text
```

---

### **روند امتیازدهی:**

```
1. کارشناس مشخصات تحصیلی کارشناسی را بررسی می‌کند
   ├─ معدل را چک می‌کند → امتیاز معدل (0-6)
   ├─ دانشگاه را چک می‌کند → امتیاز دانشگاه
   └─ جمع امتیاز = gpa_score + university_score

2. کارشناس مشخصات تحصیلی ارشد را بررسی می‌کند
   ├─ معدل را چک می‌کند → امتیاز معدل (0-6)
   ├─ دانشگاه را چک می‌کند → امتیاز دانشگاه
   └─ جمع امتیاز = gpa_score + university_score
```

---

## 🎤 **بخش 2: سیستم مصاحبه**

### **مدل Interview**

```python
class Interview:
    application: OneToOne → Application
    
    # زمان‌بندی
    scheduled_date: DateTime  # تاریخ و زمان مصاحبه
    location: str  # محل برگزاری
    status: PENDING / SCHEDULED / COMPLETED / ABSENT / CANCELLED
    
    # ============================================
    # امتیازدهی مصاحبه (حداکثر 30 امتیاز)
    # ============================================
    
    # 1. تشارک در تجربه و تحلیل مسائل علمی (3 امتیاز)
    experience_analysis_score: float = 0
    
    # 2. وسعت نظر، خلاقیت و کاربردی‌بودن (3 امتیاز)
    creativity_score: float = 0
    
    # 3. شخصیت، صحت و حجم بیان (3 امتیاز)
    personality_expression_score: float = 0
    
    # 4. نگارش و مدارک فراواحد مرتبط با رشته (3 امتیاز)
    documentation_score: float = 0
    
    # 5. برناسی فن بیان و احتمال موفق (3 امتیاز)
    speech_success_probability_score: float = 0
    
    # 6. همراستایی پژوهشی با آزمایشگاه‌های گروه (15 امتیاز)
    lab_alignment_score: float = 0
    
    # جمع امتیاز مصاحبه
    total_interview_score: float = 0  # محاسبه خودکار
    
    # اطلاعات اضافی
    interviewer_comment: Text
    interviewers: ManyToMany → User  # هیئت داوران
    conducted_at: DateTime
    scored_by: User
    scored_at: DateTime
```

---

### **جدول امتیازدهی مصاحبه (بر اساس تصویر 2)**

| ردیف | شاخص ارزیابی | حداکثر امتیاز | امتیاز ثبت شده |
|------|--------------|----------------|----------------|
| 1 | تشارک در تجربه و تحلیل مسائل علمی و پاسخگویی به سوالات | 3 | ... |
| 2 | وسعت نظر، خلاقیت و کاربردی‌بودن | 3 | ... |
| 3 | شخصیت، صحت و حجم بیان | 3 | ... |
| 4 | نگارش و مدارک فراواحد مرتبط با رشته تحصیلی | 3 | ... |
| 5 | برناسی فن بیان و احتمال موفق | 3 | ... |
| 6 | همراستایی پژوهشی داوطلب با آزمایشگاه‌های علمی تخصصی گروه | 15 | ... |
| **جمع** | - | **30** | - |

---

### **روند مصاحبه:**

```
1️⃣ ایجاد رکورد مصاحبه
   └─ Interview.objects.create(application=app, status='PENDING')

2️⃣ تعیین زمان مصاحبه
   ├─ scheduled_date = تاریخ و زمان
   ├─ location = محل برگزاری
   └─ status = 'SCHEDULED'

3️⃣ ارسال دعوتنامه به داوطلب
   └─ ایمیل/پیامک با جزئیات مصاحبه

4️⃣ انجام مصاحبه
   ├─ حضور داوطلب
   ├─ مصاحبه توسط هیئت داوران (interviewers)
   └─ status = 'COMPLETED'

5️⃣ امتیازدهی توسط کارشناس
   ├─ ثبت امتیاز هر شاخص (1-6)
   ├─ محاسبه خودکار total_interview_score
   ├─ scored_by = کارشناس
   └─ scored_at = تاریخ ثبت

6️⃣ محاسبه امتیاز نهایی داوطلب
   └─ جمع کل = امتیاز تحصیلی + سوابق پژوهشی + مصاحبه
```

---

## 📊 **بخش 3: محاسبه امتیاز نهایی**

### **فرمول امتیاز کل:**

```
امتیاز نهایی داوطلب = 
    امتیاز کارشناسی (gpa + university) +
    امتیاز ارشد (gpa + university) +
    سوابق پژوهشی (حداکثر 58 امتیاز):
        ├─ مقالات پژوهشی + اختراع + جشنواره (40)
        ├─ مقالات ترویجی (6)
        ├─ مقالات کنفرانس (4)
        ├─ کتاب (4)
        └─ پایان‌نامه (4)
    +
    مصاحبه (حداکثر 30 امتیاز)
```

---

### **مدل Application - فیلد امتیاز نهایی:**

```python
class Application:
    # امتیازهای جزئی
    education_total_score: float = 0  # جمع امتیاز تحصیلی
    research_total_score: float = 0   # جمع امتیاز پژوهشی
    interview_total_score: float = 0  # امتیاز مصاحبه
    
    # امتیاز نهایی
    total_score: float = 0
    score_calculated_at: DateTime
    
    def calculate_final_score(self):
        """محاسبه امتیاز نهایی"""
        # جمع امتیاز تحصیلی
        education_score = sum([
            record.total_score 
            for record in self.education_records.all()
        ])
        
        # جمع امتیاز پژوهشی
        research_score = (
            sum([a.score for a in self.research_articles.all()]) +
            sum([p.score for p in self.patents.all()]) +
            sum([f.score for f in self.festival_awards.all()]) +
            sum([c.score for c in self.conference_articles.all()]) +
            sum([b.score for b in self.books.all()]) +
            (self.masters_thesis.score if self.masters_thesis else 0)
        )
        
        # امتیاز مصاحبه
        interview_score = (
            self.interview.total_interview_score 
            if hasattr(self, 'interview') else 0
        )
        
        self.education_total_score = education_score
        self.research_total_score = research_score
        self.interview_total_score = interview_score
        self.total_score = education_score + research_score + interview_score
        self.score_calculated_at = timezone.now()
        
        return self.total_score
```

---

## 🎯 **وضعیت‌های پرونده در روند دکتری**

```
NEW → ثبت‌نام اولیه
    ↓
PROGRAM_SELECTED → انتخاب رشته
    ↓
PERSONAL_INFO_COMPLETED → اطلاعات شخصی تکمیل شد
    ↓
IDENTITY_DOCS_UPLOADED → مدارک شناسایی آپلود شد
    ↓
EDU_INFO_COMPLETED → اطلاعات تحصیلی تکمیل شد
    ↓
EDU_DOCS_UPLOADED → مدارک تحصیلی آپلود شد
    ↓
SUBMITTED → ارسال شده (منتظر بررسی)
    ↓
UNDER_UNIVERSITY_REVIEW → در حال بررسی مسئول دانشگاه
    ↓
[تایید] → APPROVED_BY_UNIVERSITY
    ↓
SCORING_IN_PROGRESS → در حال امتیازدهی توسط کارشناس
    ├─ امتیازدهی تحصیلی
    ├─ امتیازدهی پژوهشی
    └─ محاسبه جمع
    ↓
INTERVIEW_SCHEDULED → زمان مصاحبه تعیین شد
    ↓
INTERVIEW_COMPLETED → مصاحبه انجام شد
    ↓
FINAL_SCORE_CALCULATED → امتیاز نهایی محاسبه شد
    ↓
UNDER_FACULTY_REVIEW → در حال بررسی مسئول دانشکده
    ↓
FACULTY_REVIEW_COMPLETED → بررسی دانشکده تکمیل شد
    ↓
COMPLETED → تکمیل شده (نتیجه اعلام شد)
```

---

## 📝 **API Endpoints مورد نیاز**

### **امتیازدهی کارشناس:**

```
# امتیازدهی تحصیلی
POST   /api/admin/education-records/{id}/score/
{
    "gpa_score": 6,
    "university_score": 2,
    "review_comment": "معدل عالی"
}

# امتیازدهی پژوهشی (قبلاً اضافه شده)
POST   /api/admin/research-articles/{id}/score/
POST   /api/admin/patents/{id}/score/
...

# مدیریت مصاحبه
POST   /api/admin/interviews/
GET    /api/admin/interviews/{id}/
PATCH  /api/admin/interviews/{id}/schedule/
PATCH  /api/admin/interviews/{id}/score/
{
    "experience_analysis_score": 2.5,
    "creativity_score": 3,
    "personality_expression_score": 2,
    "documentation_score": 2.5,
    "speech_success_probability_score": 3,
    "lab_alignment_score": 12,
    "interviewer_comment": "عملکرد خوب"
}

# محاسبه امتیاز نهایی
POST   /api/admin/applications/{id}/calculate-final-score/
```

---

## ✅ **خلاصه تغییرات**

### **مدل ApplicationEducationRecord:**
- ✅ `gpa_score` - امتیاز معدل
- ✅ `university_score` - امتیاز دانشگاه
- ✅ `total_score` - جمع امتیاز این مقطع
- ✅ `reviewed_by`, `reviewed_at`, `review_comment`

### **مدل Interview (جدید):**
- ✅ زمان‌بندی مصاحبه
- ✅ 6 شاخص امتیازدهی
- ✅ جمع خودکار امتیازها
- ✅ هیئت داوران (ManyToMany)
- ✅ نظرات و توضیحات

### **مدل Application:**
- ✅ `education_total_score`
- ✅ `research_total_score`
- ✅ `interview_total_score`
- ✅ متد `calculate_final_score()`

---

## 🎉 **وضعیت پیاده‌سازی**

- [x] مدل Interview
- [x] فیلدهای امتیازدهی در ApplicationEducationRecord
- [x] Admin panel برای Interview
- [x] محاسبه خودکار جمع امتیاز مصاحبه
- [ ] Serializers
- [ ] API Views
- [ ] محاسبه امتیاز نهایی در Application

---

**🚀 سیستم امتیازدهی و مصاحبه با موفقیت پیاده‌سازی شد!**
