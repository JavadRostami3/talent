# بررسی آماده بودن ثبت‌نام

## پاسخ سوال شما

**سوال:** آیا وقتی دکمه ثبت‌نام را در فرانت می‌زنم، درست با بک‌اند ارتباط برقرار می‌شود و در دیتابیس ذخیره می‌شود؟

**پاسخ:** **بله، اما...** یک شرط دارد! ✅

## ✅ چیزهایی که آماده هستند:

1. ✅ **API Endpoint**: `/api/auth/register/` کاملاً پیاده‌سازی شده
2. ✅ **Frontend**: صفحه Register.tsx به API متصل است
3. ✅ **Models**: تمام مدل‌ها آماده هستند (User, ApplicantProfile, Application)
4. ✅ **Validation**: اعتبارسنجی‌ها درست کار می‌کنند

## ⚠️ شرط مهم

برای اینکه ثبت‌نام کار کند، **باید یک فراخوان فعال (AdmissionRound) در دیتابیس وجود داشته باشد!**

### چرا؟

API در هنگام ثبت‌نام بررسی می‌کند که آیا فراخوان فعالی با `type` مورد نظر وجود دارد یا نه. اگر نباشد، خطا می‌دهد:

```
{'error': 'فراخوان فعالی برای این نوع یافت نشد'}
```

## 🚀 راه‌حل: ایجاد فراخوان‌های تست

### روش سریع (پیشنهادی):

دستور زیر را اجرا کنید:

```bash
cd backend
python create_test_rounds.py
```

این دستور 4 فراخوان تست برای شما ایجاد می‌کند:
- ✅ MA_TALENT (استعداد درخشان ارشد)
- ✅ PHD_TALENT (استعداد درخشان دکتری)
- ✅ PHD_EXAM (آزمون دکتری)
- ✅ OLYMPIAD (المپیاد)

### روش دستی (از Admin):

1. سرور را راه‌اندازی کنید:
   ```bash
   python manage.py runserver
   ```

2. به پنل ادمین بروید:
   ```
   http://localhost:8000/admin/
   ```

3. با سوپریوزر وارد شوید

4. به بخش "فراخوان‌های پذیرش" بروید

5. یک فراخوان جدید ایجاد کنید و **فعال** کنید

## 📋 چک‌لیست قبل از تست

- [ ] فراخوان‌های تست ایجاد شده‌اند
- [ ] سرور بک‌اند در حال اجرا است (`python manage.py runserver`)
- [ ] سرور فرانت در حال اجرا است (`npm run dev` در پوشه frontend)
- [ ] CORS تنظیم شده (در settings.py)

## 🧪 تست ثبت‌نام

بعد از ایجاد فراخوان‌ها:

1. به صفحه ثبت‌نام بروید: http://localhost:8080/register

2. فرم را پر کنید:
   - کد ملی: 770231543 (یا هر کد ملی معتبر 10 رقمی)
   - نام: تست
   - نام خانوادگی: تست
   - موبایل: 09123456789
   - ایمیل: test@test.com
   - نوع فراخوان: یکی از انواع را انتخاب کنید

3. روی دکمه "ثبت‌نام" کلیک کنید

4. اگر موفق باشد، باید:
   - ✅ کد پیگیری نمایش داده شود
   - ✅ در دیتابیس ذخیره شود

## 🔍 بررسی ذخیره شدن در دیتابیس

برای بررسی اینکه آیا در دیتابیس ذخیره شده:

```bash
python manage.py shell
```

```python
from apps.accounts.models import User
from apps.applications.models import Application

# بررسی کاربران
users = User.objects.all()
print(f"تعداد کاربران: {users.count()}")
for user in users:
    print(f"  - {user.get_full_name()} ({user.national_id})")

# بررسی پرونده‌ها
apps = Application.objects.all()
print(f"\nتعداد پرونده‌ها: {apps.count()}")
for app in apps:
    print(f"  - کد پیگیری: {app.tracking_code}")
    print(f"    داوطلب: {app.applicant.user.get_full_name()}")
    print(f"    فراخوان: {app.round.title}")
    print()

exit()
```

## 📝 خلاصه

| مورد | وضعیت |
|------|-------|
| API Endpoint | ✅ آماده |
| Frontend | ✅ آماده |
| Models | ✅ آماده |
| **فراخوان فعال** | ⚠️ **نیاز به ایجاد** |

## نتیجه

**بله، ثبت‌نام کار می‌کند!** اما فقط بعد از اینکه فراخوان‌های تست را ایجاد کنید. 🎉

---

**فایل‌های مفید:**
- `backend/CHECK_REGISTRATION.md` - راهنمای کامل
- `backend/create_test_rounds.py` - اسکریپت ایجاد فراخوان‌های تست

